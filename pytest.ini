[pytest]
# `pytest` a secas no mete el directorio actual en sys.path; `python -m
# pytest` sí. Sin esto, en CI fallaba la importación de `app` aunque en
# local pasara. Fijarlo aquí hace que las dos formas se comporten igual.
pythonpath = .
testpaths = tests
